import 'package:flutter/material.dart';

/// شاشة اللعبة المصغرة (Aim Trainer) — Placeholder، تُبنى لاحقاً.
class AimTrainerScreen extends StatelessWidget {
  const AimTrainerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Aim Trainer')),
      body: const Center(child: Text('قريباً 🎯')),
    );
  }
}
