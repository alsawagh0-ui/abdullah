<div dir="rtl">

# حالة المشروع ونقطة الاستئناف

> آخر تحديث: 31 أغسطس 2026 — الفرع `claude/wajib-social-ai-platform-ve69ks`

## أين وصلنا

| العنصر | الحالة |
|---|---|
| وثيقة المشروع | مكتملة — [`README.md`](README.md)، مدموجة في `main` |
| تطبيق Flutter (iOS + Android + Web) | نموذج عامل كامل المنطق — [`app/`](app/) |
| الموقع التعريفي | جاهز — [`website/`](website/)، ولقطاته الآن لقطات حقيقية من التطبيق نفسه |
| الاختبارات | **186 اختباراً ناجحاً**، `flutter analyze` بلا ملاحظات |
| Pull Request | [#2](https://github.com/alsawagh0-ui/abdullah/pull/2) — **مدموج** |
| بناء APK تلقائي | ✅ **يعمل فعلياً الآن** — [`.github/workflows/build-apk.yml`](.github/workflows/build-apk.yml)، أول بناء ناجح: [تشغيل #5](https://github.com/alsawagh0-ui/abdullah/actions/runs/33358533096) |
| نشر معاينة الويب | **تم إنشاؤه** — [`.github/workflows/deploy-pages.yml`](.github/workflows/deploy-pages.yml) ينشر الموقع التعريفي + نسخة ويب تفاعلية من التطبيق على GitHub Pages عند push إلى `main` أو تشغيل يدوي |
| التشغيل على جهاز حقيقي | **لم يحدث بعد على جهاز فعلي** — لكن الـ APK جاهز للتنزيل الآن من مرفقات تشغيل Actions، أو جرّب نسخة الويب مباشرة من المتصفح |

## الخطوات القادمة — مرتّبة حسب الأثر

### 1. احصل على APK وجرّب التطبيق بيدك — ✅ تم فعلياً
`build-apk.yml` نجح فعلياً على Android SDK حقيقي على Actions. تحميل
الـ APK: افتح
[تشغيل #5](https://github.com/alsawagh0-ui/abdullah/actions/runs/33358533096)
→ Artifacts → `wajib-release-apk` (36 م.ب تقريباً)، ثبّته على جهاز
أندرويد يسمح بمصادر غير معروفة.

استغرق الوصول لهذه النتيجة إصلاح عطلين حقيقيين ظهرا بالتتابع عند أول
بناء فعلي للمشروع على الإطلاق:

1. **`flutter_tesseract_ocr` غير متوافقة مع Gradle 9 / AGP 9.** حزمة
   الاستخلاص الضوئي (آخر تحديث لها قديم) يحتوي `android/build.gradle`
   بأسلوب Groovy كلاسيكي يستدعي `jcenter()` (دالة محذوفة من
   `RepositoryHandler` في Gradle 9.3.1) ويثبّت classpath خاص بـ AGP
   7.1.2 غير متوافق أصلاً مع Gradle 9. **الإصلاح:** نسخة محلية مصححة
   من نفس إصدار الحزمة (0.4.31) في
   [`app/third_party/flutter_tesseract_ocr`](app/third_party/flutter_tesseract_ocr)
   — الفرق الوحيد أن `android/build.gradle` يطبّق `com.android.library`
   بلا رقم إصدار مثبّت، فتُحل تلقائياً عبر `pluginManagement` في
   `app/android/settings.gradle.kts` بدل حمل AGP خاص بها. مربوطة عبر
   `dependency_overrides` في `pubspec.yaml`.
2. **`flutter_local_notifications` يحتاج core library desugaring.**
   خطأ منفصل تماماً ظهر بعد حل المشكلة الأولى، في خطوة
   `checkReleaseAarMetadata`. الإصلاح سطران في
   `app/android/app/build.gradle.kts`:
   `isCoreLibraryDesugaringEnabled = true` + تبعية
   `com.android.tools:desugar_jdk_libs`.

كلا الإصلاحين تم التحقق منهما فعلياً على Actions (لا يوجد Android SDK
في بيئة التطوير هنا)، بعد محاولتين فاشلتين أولى (خطأ ترتيب
`plugins {}` في الملف المصحح، ثم اكتشاف مشكلة الـ desugaring).

بديل فوري بلا انتظار: نسخة الويب المنشورة عبر `deploy-pages.yml` تعمل
من أي متصفح فوراً (بها نفس منطق التطبيق، فيما عدا الاستخلاص الضوئي
وبعض تكاملات الجهاز التي تحتاج منصة أصلية).

### 2. جرّب الاستخلاص الضوئي على 5 بطاقات نعي حقيقية
**أخطر مجهول تقني في المشروع.** بطاقات النعي بخطوط زخرفية، وهي أصعب
حالة لأي محرك ضوئي. نتيجة هذه التجربة تحدد إن كان Tesseract كافياً، أم
يحتاج معالجة مسبقة للصور، أم يحتاج إعادة نظر كاملة.

### 3. مقابلات ميدانية — أهم من الشيفرة
«المرحلة 0 — التحقق» في الوثيقة. أكبر مخاطرة في المشروع ليست تقنية:

- هل «الدَّين الاجتماعي غير المرئي» ألم يشعر به الناس فعلاً؟
- وهل يتقبّلون **رقمنة العزاء** تحديداً؟ أخطر نقطة في الفكرة كلها.

10–15 شخصاً من فئات مختلفة (رب أسرة، موظف، امرأة مسؤولة عن التواصل
العائلي، رجل فوق الستين)، مع مراقبة ردة الفعل على شاشة العزاء تحديداً.

### 4. قرارات مؤجلة حتى تنتهي 1–3
الخادم والحسابات ورمز التحقق · الهوية البصرية وأيقونة التطبيق ·
مصدر التقويم الهجري الرسمي · التوجه لجولة التمويل بنموذج أم بأرقام
استخدام حقيقية. **لا يُحسم شيء منها قبل نتائج المقابلات.**

## فجوات معروفة في التطبيق

| الفجوة | الوضع |
|---|---|
| الحساب والدخول | رقم الهاتف يُحفظ محلياً، بلا رمز تحقق حقيقي ولا خادم |
| الإدخال الصوتي | **منفَّذ الآن** عبر حزمة `speech_to_text` (محرك النظام). **تنبيه ستر لم يُحسم:** خلافاً لقراءة الصور (Tesseract محلي بالكامل)، هذا يستخدم محرك التعرّف الكلامي الافتراضي للنظام وهو غالباً يرسل الصوت لخادم آبل/جوجل إلا على الأجهزة التي تدعم التعرّف محلياً. لم يُختبر بعد على جهاز حقيقي (بيئة التطوير بلا Android SDK) — انظر تفصيل القرار في `app/lib/services/platform_services.dart` |
| سوق الموردين | قائمة ثابتة للعرض، بلا طلبات ولا دفع |
| الإنابة | تُسجَّل محلياً، لا تصل للشخص المُناب |
| البيانات | محلية بلا نسخ احتياطي ولا مزامنة بين الأجهزة. **تحسين أمني:** كانت تُحفظ بنص صريح غير مُعمّى في SharedPreferences رغم أنها خريطة العلاقات ودفتر المعاملة بالمثل وأرقام التواصل — تناقض مباشر مع مبدأ الستر. الآن تُحفظ عبر `flutter_secure_storage` (Keychain/Keystore مُعمّى)، مع ترحيل تلقائي للبيانات القديمة عند أول تشغيل بعد التحديث |
| الأيقونة | أيقونة Flutter الافتراضية |
| التقويم الهجري | خوارزمية جدولية قد تفارق أم القرى بيوم |

## ملاحظات بيئة التطوير (توفّر وقتاً عند الاستئناف)

- **Flutter 3.47.2 / Dart 3.13.2** — يُثبَّت بـ
  `git clone --depth 1 -b stable https://github.com/flutter/flutter.git /opt/flutter`
- **لا يمكن بناء APK في بيئة Claude Code على الويب**: لا Android SDK،
  وتنزيله من `dl.google.com` محجوب بسياسة الشبكة (403).
- **iOS** يحتاج macOS و Xcode — غير متاح على Linux إطلاقاً.
- **للتحقق البصري**: تُنسخ نسخة ويب مؤقتة **خارج المستودع**، ويُضاف
  `--platforms=web`، ثم يُوجَّه CanvasKit للنسخة المحلية بتعديل
  `flutter_bootstrap.js` (شبكة `gstatic` محجوبة)، ويُشغَّل المتصفح
  بـ `locale: 'ar-KW'` وإلا فشل التطبيق بخطأ locale.

## قرار تقني مهم موثّق

**ML Kit من جوجل لا يدعم العربية إطلاقاً** (لاتيني، صيني، ياباني، كوري،
ديفاناغاري فقط). لذلك اعتُمد Tesseract على الجهاز — لا خدمة سحابية —
حفاظاً على مبدأ الستر: صورة بطاقة العزاء لا تغادر الجهاز.

</div>
